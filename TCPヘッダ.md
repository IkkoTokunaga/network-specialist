2026-03-29
# TCPヘッダ
データの送り方を制御する。オプションを除くと基本的なサイズは20バイト
## 送信元ポート番号
送信元のアプリケーションのポート番号を示す
## 宛先ポート番号
宛先ポート番号を示す
例：HTTP＝80、ssh＝22
## シーケンス番号
送信するデータの位置を示す。
バラバラに届くデータはシーケンス番号を参照して並び替えられる
## 確認応答番号（ACK番号）
次にほしいデータの番号を示す。シーケンス番号の次の番号になる
この番号を返すことでデータの到達を確認し信頼性を担保する